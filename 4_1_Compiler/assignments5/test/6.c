a = 5.0.1; // 잘못된 실수 표현
c = "z"; // 잘못된 문자 할당
arr[10] = 1; // 배열 인덱스 초과
ptr = &&a; // 잘못된 주소 연산자 사용
if (a > ) { // 조건문 오류
    a = a + 1;
}
while (b < ) { // 반복문 오류
    b = b + 0.1;
}
for (int i = 0; i < 10 i++) { // 반복문 구문 오류
    arr[i] = i;
}